name: pr-hierarchy

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - edited

jobs:
  enforce-pr-hierarchy:
    permissions:
      contents: write
      pull-requests: write

    uses: tspyder7/github-actions-lib/.github/workflows/enforce-pr-hierarchy.yml@ci/optimize-logging-enforce-pr-hierarchy
    with:
      allowed-branches-hierarchy-json: |
        {
            "release": [
                "main"
            ],
            "main": [
                "stage"
            ],
            "stage": [
                "development"
            ]
        }
    secrets:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
